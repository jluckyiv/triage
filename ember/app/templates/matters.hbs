<div class="page-header">
  <h2 id="matters-title">Triage Matters
    <small>
      {{filterValue}} cases for
      {{dateString}}
    </small>
  </h2>
</div>
{{partial 'filter-bar'}}
{{#each filtered}}
{{render 'matter' this}}
{{else}}
<hr />
<h4>None</h4>
{{/each}}

