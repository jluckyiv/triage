<div class="col-sm-3">
  <div class="btn-group dropdown-parent">
    <button {{action "pausePolling"}} {{bind-attr class="petitionerHasAppeared:btn-success:btn-warning :petitioner-button :btn :dropdown-toggle"}} data-toggle="dropdown">
      {{petitioner}} <span class="caret"></span>
    </button>
    {{#if petitionerHasAppeared}}
    <ul class="dropdown-menu" role="menu">
      <li><a {{action "petitionerDisappeared" this }}>Check out {{petitioner}}</a></li>
    </ul>
    {{else}}
    <ul class="dropdown-menu" role="menu">
      <li><a {{action "petitionerAppeared" this }}>Check in {{petitioner}}</a></li>
    </ul>
    {{/if}}
  </div>
</div>


